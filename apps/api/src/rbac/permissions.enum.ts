/**
 * Re-exports from `@pjvpin/shared` para mantener la API publica del modulo
 * (los consumidores siguen importando de `../rbac/permissions.enum`).
 */
export { AppPermission } from "@pjvpin/shared";

